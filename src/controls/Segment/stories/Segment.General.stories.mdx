import {ArgsTable, Meta, Canvas, Story} from "@storybook/addon-docs";
import {Segment} from '../Segment';

# Segment: General
.

<Meta
  title="Controls/Segment/General"  
  component={Segment}
  argTypes={{
    className: { table: { disable: true } }
  }}
/>

export const Template = (args) => 
  <Segment {...args}/>

# disabled

A `Segment` may show that its content is `disabled`.

<Canvas>
  <Story
    name="disabled"
    args={{ disabled: true, children: "This content is disabled." }}>
    {Template.bind({})}
  </Story>
</Canvas>
