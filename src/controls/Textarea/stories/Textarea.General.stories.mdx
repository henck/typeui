import {ArgsTable, Meta, Canvas, Story} from "@storybook/addon-docs";
import {Textarea} from '../Textarea';
import {Form} from '../../Form';

# Textarea: General
. 

<Meta
  title="Controls/Textarea/General"  
  component={Textarea}
  argTypes={{
    className: { table: { disable: true } }
  }}
/>

export const Template = (args) =>
  <Form data={{}} onChange={() => {}} onValidate={() => {}}>
    <Form.Field
      name="name"
      value={null}
      control={<Textarea {...args}/>}
    />
  </Form>

## minHeight 

A textarea can be given a `minHeight` measured in pixels.

Note that this works together with `rows`, whichever yields the greatest height.

<Canvas>
  <Story
    name="minHeight"
    args={{ minHeight: 180, rows: null }}>
    {Template.bind({})}
  </Story>
</Canvas>

## rows 

A textarea can be given a height measured in `rows`.

Note that this works together with `minHeight`, whichever yields the greatest 
height.

<Canvas>
  <Story
    name="rows"
    args={{ minHeight: null, rows: 8 }}>
    {Template.bind({})}
  </Story>
</Canvas>

## disabled 

A `Textarea` can be `disabled`.

<Canvas>
  <Story
    name="disabled"
    args={{ disabled: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

## error

A `Textarea` can be in an `error` state. 

<Canvas>
  <Story
    name="error"
    args={{ error: true }}>
    {Template.bind({})}
  </Story>
</Canvas>
