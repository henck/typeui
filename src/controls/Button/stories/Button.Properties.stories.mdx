import {ArgsTable, Meta, Canvas, Story} from "@storybook/addon-docs";
import {Button} from '../Button';
import {Icon} from '../../Icon';
import {Segment} from '../../Segment';
import {Divider} from '../../Divider';
import {Label} from '../../Label';

# Button 

A `Button` replaces the HTML `<button>` element.

<Meta 
  title="Controls/Button" 
  component={Button}
  argTypes={{
    className: { table: { disable: true } },
    grouped: { table: { disable: true } }    
  }}
/>

export const Template = (args) => <Button {...args}>Button</Button>

<ArgsTable of={Button}/>

## Default button 

By default, `Button` controls have an ink ripple effect, which can be disabled. 
Buttons can be grouped in a `Button.Group` or presented as options using 
`Button.Or`.

<Canvas>
  <Story
    name="Properties"
    args={{ }}>
    {Template.bind({})}
  </Story>
</Canvas>

