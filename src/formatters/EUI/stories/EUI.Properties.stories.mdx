import {ArgsTable, Meta, Canvas, Story} from "@storybook/addon-docs";
import { EUI } from '../EUI';

<Meta
  title="Formatters/EUI"  
  component={EUI}
  argTypes={{
    className: { table: { disable: true } }
  }}
/>

# EUI

`EUI` formats an extended unique identifier. This formatter can take either a string 
or a number. A number will be converted to a hexadecimal string; a string is presented
as-is.

<ArgsTable of={EUI}/>

## Example

export const Template = (args) =>
<EUI {...args}/>

<Canvas>
  <Story
    name="Properties"
    args={{ value: "12345678abcdefaa" }}>
    {Template.bind({})}
  </Story>
</Canvas>

