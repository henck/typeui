import {ArgsTable, Meta, Canvas, Story} from "@storybook/addon-docs";
import { Template } from './Template.stories'

# Dark theme

<Meta
  title="Theme/Themes"  
  argTypes={{
    className: { table: { disable: true } }
  }}
/>

<Canvas>
  <Story
    name="Dark theme"
    args={{ fontColor: '#fff', background: '#333', normalColor: '#666' }}>
    {Template.bind({})}
  </Story>
</Canvas>

